import 'package:flutter/material.dart';

final dialogBackgroundColor = Colors.white60.withOpacity(0.2);

final touchFeedbackColor = Colors.white60;

final timeSettingDisplayTextStyle = TextStyle(fontSize: 18.0);

final statusUpperTextStyle = TextStyle(fontSize: 24.0);

final statusLowerTextStyle = TextStyle(fontSize: 48.0, fontWeight: FontWeight.bold);

const double largePlayIconSize = 200.0;

const double smallPlayIconSize = 54.0;
