import 'package:flutter/material.dart';

class PlayButtonsPart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("PlaysButtonsPart")),
    );
  }
}
