enum HeaderType {
  LEVEL,
  THEME,
  TIME,
}

enum RunningStatus {
  BEFORE_START,
  ON_START,
  INHALE,
  HOLD,
  EXHALE,
  PAUSE,
  FINISHED,
}

const int INITIAL_INTERVAL = 3;

enum PurchaseMode {
  DONATE,
  DELETE_AD,
  SUBSCRIPTION,
}
